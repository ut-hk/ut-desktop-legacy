import { InternalImagePipe } from './internal-image.pipe';

describe('InternalImagePipe', () => {
  it('create an instance', () => {
    const pipe = new InternalImagePipe();
    expect(pipe).toBeTruthy();
  });
});
